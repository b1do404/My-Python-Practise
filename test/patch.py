# ملف: patcher.py
import os


def patch_binary_file():
    file_path = "game.bin"  # اسم الملف اللي عايزين نعدله

    # الكلمة الجديدة اللي هنحطها (HACK)
    new_bytes = b"\x48\x41\x43\x4b"

    try:
        # r+b = فتح الملف للقراءة والكتابة بصيغة ثنائية
        with open(file_path, "r+b") as f:
            # التحرك لأول الملف (المكان رقم 0)
            f.seek(0)

            # الكتابة فوق البيانات القديمة
            f.write(new_bytes)

            print(f"😈 تم حقن الكود بنجاح! تم تغيير بداية الملف إلى: {new_bytes}")

    except FileNotFoundError:
        print("❌ ملف اللعبة مش موجود، شغل الكود الأولاني الأول!")


if __name__ == "__main__":
    patch_binary_file()
